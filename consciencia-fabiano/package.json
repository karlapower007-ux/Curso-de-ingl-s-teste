{
  "name": "consciencia-do-fabiano",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check public/app.js && npx wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "build": "npm run check",
    "deploy": "wrangler deploy",
    "cf:bootstrap": "npm run check",
    "cf:deploy": "npm run check && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
