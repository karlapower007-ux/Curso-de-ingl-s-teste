{
  "name": "consciencia-do-fabiano",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check src/index.js && node --check public/app.js",
    "deploy": "wrangler deploy",
    "migrate:remote": "wrangler d1 execute consciencia-fabiano-rag-db --remote --file=./migrations/0001_init.sql",
    "build": "npm run cf:bootstrap",
    "cf:bootstrap": "bash ./scripts/workers-builds-bootstrap.sh",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
